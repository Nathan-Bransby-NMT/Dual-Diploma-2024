def tim_sort(data: list) -> list:
    # todo - implement from https://www.geeksforgeeks.org/timsort/
    if len(data) <= 1:
        # Nothing to sort against
        return data
    # min_run = ... todo -- <= the power on 2...
    quater_length,
    try:
        # devide array into 
        ...
    except ValueError as e:
        raise ValueError(f"Unable to compare data content: {e}" from e)
    