from typing import Optional


def tim_sort(data: list, runs: Optional[int] = None) -> list:
    """Converts
    """
    # todo - implement from https://www.geeksforgeeks.org/timsort/
    if len(data) <= 1:
        # Nothing to sort against
        return data
    elif 1 < len(data) < 17:
        ...
    # min_run = ... todo -- <= the power on 2...
    #quater_length
    try:
        # devide array into 
        ...
    except ValueError as e:
        raise ValueError(f"Unable to compare data content.")
    